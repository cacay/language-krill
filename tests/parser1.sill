
{- Comment -}

{- Nested {- nest {- nest -} -} comment -}

module Main where

  term : 1
  `c <- term = close `c

  take : 1 -o 1
  `c <- take = do
    `d <- recv `c
    wait `d
    close `c

  test : 1
  `d <- test = do close `d

module Test where
